<div class="mx-auto md:w-2/3 w-full h-full">
  <h1 class="font-bold text-4xl">Editando producto</h1>

  <%= render "form", product: @product %>

  <%= render "selectCategories", product: @product %>

</div>
